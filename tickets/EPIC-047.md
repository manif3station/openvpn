# EPIC-047

## Title

Add a standalone Go mirror to `openvpn`.

## Goal

Provide a Go implementation of the `openvpn` command set that can run without the Developer Dashboard runtime while preserving the same credential, MFA, reconnect, and process-management behavior.

## Status

Done
