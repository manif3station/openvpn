# EPIC-044

## Title

Bootstrap the `openvpn` reconnect skill.

## Goal

Create a DD skill that can store OpenVPN credentials, build the password plus 2FA suffix on demand, and keep the tunnel connected through a collector-managed reconnect loop.
