# DD-074

## Title

Build `go-version/` as a four-command mirror for `openvpn`.

## Method

TDD

## Acceptance

- `skills/openvpn/go-version/` contains a standalone Go implementation of `setup`, `connect`, `disconnect`, and `noreconnect`
- the Go implementation reads `~/.openvpn.env` using the canonical `USERNAME`, `PASSWORD`, `MFA`, and `CONFIG` keys and keeps compatibility with legacy `OPENVPN_*` keys
- the Go implementation mirrors the managed auth-file, state-file, reconnect, and TOTP behavior of the existing Perl skill
- the Go implementation can be built into four Windows CLI binaries named after the existing Perl entrypoints
- Linux and macOS code paths remain supported in the Go implementation even though no live host integration proof is required for this ticket
- Docker tests and 100% coverage for the Go implementation pass
- docs, commit, and push are completed

## Status

Done
