# DD-073

## Title

Add Windows-oriented env aliases, config layout, and no-popup auth flow to `openvpn`.

## Method

TDD

## Acceptance

- `dashboard openvpn.setup` accepts and stores the Windows-oriented credential contract using `USERNAME`, `PASSWORD`, and `MFA` in `~/.openvpn.env`
- the skill still reads the existing `OPENVPN_USERNAME`, `OPENVPN_PASSWORD`, and `OPENVPN_2FA` values for backward compatibility
- the skill supports `~/openvpn/config/` as a first-class config location and runtime helper location on Windows
- the generated auth-user-pass flow keeps OpenVPN from prompting for username and password again during managed connects
- Linux, macOS, and Windows code paths remain covered by automated tests
- Docker tests and 100% coverage pass
- docs, commit, and push are completed

## Status

Done
