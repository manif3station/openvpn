# EPIC-046

## Title

Add Windows auto-login compatibility to `openvpn`.

## Goal

Extend the existing `openvpn` skill so it can drive an already installed OpenVPN client on Windows with stored username, password, and MFA state while preserving Linux and macOS support.

## Status

Done
