Revision history for openvpn

0.07 2026-05-05
    - kept the raw `MFA` secret or static suffix in `~/.openvpn.env` and clarified that `auth.txt` stores the generated current code for the current connect attempt only
    - improved the standalone Go Windows launcher so it waits for the managed OpenVPN process and accepts a live spawned pid when the pid file is delayed
    - added `log_file` to the Go mirror JSON payload and included log-path context in Windows connect failure messages
    - reverified the standalone Go mirror in Docker at `100.0%` statement coverage after the Windows connect fix

0.06 2026-05-05
    - added `go-version/` as a standalone Go mirror for `setup`, `connect`, `disconnect`, and `noreconnect`
    - kept the Go mirror source-only in the repository while documenting build scripts that produce the Windows command-shaped executables on demand
    - mirrored the canonical `USERNAME`, `PASSWORD`, `MFA`, and `CONFIG` env contract and legacy `OPENVPN_*` compatibility in the Go implementation
    - added Docker-verified Go tests with `100.0%` statement coverage for the standalone mirror package

0.05 2026-05-05
    - added the short `USERNAME`, `PASSWORD`, `MFA`, and `CONFIG` env contract while keeping legacy `OPENVPN_*` keys readable
    - added `-c` and `--config` to `dashboard openvpn.setup`
    - promoted `~/openvpn/config/` to a first-class config discovery location
    - moved the default Windows runtime helper directory to `~/openvpn/config/dd-runtime`
    - started OpenVPN with `--auth-retry nointeract` so the managed CLI path does not reopen username/password prompts

0.04 2026-05-01
    - expanded `cpanfile` so it explicitly records the core Perl modules used by the skill and names the skill-local modules separately

0.03 2026-05-01
    - added an explicit `cpanfile` to keep the dependency gate present even though the skill currently uses only Perl core modules and skill-local modules

0.02 2026-05-01
    - created the initial `openvpn` skill with setup, connect, disconnect, and noreconnect commands
    - added collector-backed OpenVPN connection monitoring with auto reconnect and retry exhaustion handling
    - stored OpenVPN credentials in `~/.openvpn.env` and generated auth-user-pass files from that state
    - added TOTP and `otpauth://` secret handling for six-digit reconnect suffix generation
    - removed package-manager installation of `openvpn` and moved process control into skill-owned Perl modules
    - added Windows 11 PowerShell-aware launcher behavior for `openvpn.exe`, pid checks, and config discovery
