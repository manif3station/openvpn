# 2026-05-05 Go Mirror

## Summary

Added a standalone Go mirror under `go-version/` for `setup`, `connect`, `disconnect`, and `noreconnect`.

## What Changed

- created a Go command mirror that reads `~/.openvpn.env` with the canonical `USERNAME`, `PASSWORD`, `MFA`, and `CONFIG` keys
- kept legacy `OPENVPN_*` compatibility in the Go implementation
- mirrored the same auth-file, reconnect, state-file, TOTP, and OpenVPN launcher behavior from the Perl skill
- added source-only build scripts for Windows command-shaped binaries instead of committing generated `.exe` files into the repository
- added Docker-verified Go tests for the standalone mirror package

## Verification

- Docker Go test command passed:

```bash
docker run --rm -v ~/projects/skills/skills/openvpn/go-version:/work -w /work golang:1.22-bookworm bash -lc '/usr/local/go/bin/go test ./mirror -coverprofile=coverage.out && /usr/local/go/bin/go tool cover -func=coverage.out'
```

- coverage result:
  - `github.mf/manif3station/openvpn/go-version/mirror` `100.0%` statements

## Notes

- `go-version/` stays source-only in Git
- generated `cli/*.exe` files are build outputs and are intentionally ignored
